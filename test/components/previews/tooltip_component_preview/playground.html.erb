<div class="w-full my-52 flex items-center justify-center">
  <%= render(FoxTail::TooltipComponent.new(placement: placement, arrow: arrow, variant: variant)) do |c| %>
    <% c.with_trigger do |trigger| %>
      <%= render FoxTail::ButtonComponent.new(trigger.html_attributes).with_content("Hover over me") %>
    <% end %>
    Tooltip Content
  <% end %>
</div>
